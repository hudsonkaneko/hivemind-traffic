# SUMO/TraCI client libraries; keep these aligned with the installed SUMO release.
sumolib==1.27.1
traci==1.27.1

# Multi-agent environment stack.
gymnasium==1.3.0
numpy==2.4.6
pettingzoo==1.27.0

# Shared PPO baseline. SUMO training uses the portable CPU wheel; Isaac Sim keeps
# its own GPU-managed Python runtime.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.14.0+cpu

# Test tooling.
pytest==9.1.1
pytest-timeout==2.4.0
